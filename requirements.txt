# FALLBACK ONLY -- for a laptop where `uv` cannot be installed.
#
# pyproject.toml is the SOURCE OF TRUTH. This file exists because pip cannot
# install dependencies out of a pyproject that declares no build backend, and
# this project deliberately declares none (it is not a package -- see the
# comment in pyproject.toml). If you edit one, edit the other.
#
# ⚠️ LAPTOP ONLY. Never run pip against this on the Jetson: OpenCV/NumPy/
# TensorFlow come from JetPack, and pip would try to build OpenCV from source
# on the Nano.
#
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # macOS / Linux
#   pip install -r requirements.txt

fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
numpy>=1.24
opencv-python-headless>=4.8
requests>=2.31

# tests
pytest>=8.0
httpx>=0.27
